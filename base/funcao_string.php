<?php
  // strtoupper($string) = tranforma string com letras maiusculas
  // strtolower($string) = tranforma string com letras miniusculas
  // substr($string, start, stop) = start define a apartir de qual caracter e o stop são quantos caracteres apartir do start (stop não é obrigatorio)
  // srt_pad($string, length, caracter, position) = Define qual o tamanho a string, e caso ela não tiver o tamanha correto, completar com um caracter especifico
  // str_repeat($string, count) = define a quantidade de vez uma palavra ira repetir dentro da string
  // strlen($string) = retorna o comprimento da string
  // str_relace($texto, $newTexto, $string) = substitui texto dentro de uma string
  // strpos($string, $texto) = retorna a posição do texto
?>